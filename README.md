# 100 DAYS OF CODE JAVA ☕

Bem-vindo ao meu repositório do #100DaysOfCode focado em Java!🎉

![image](https://github.com/user-attachments/assets/4ba416d7-58bc-4dce-ab74-38c817c09780)


Este repositório é dedicado ao desafio #100DaysOfCode, no qual me comprometi a codificar todos os dias, durante 100 dias consecutivos, focando exclusivamente em Java. O objetivo deste desafio é não apenas reforçar meus conhecimentos em Java, mas também criar uma rotina de estudos consistente e evoluir como desenvolvedor.

Ao longo desses 100 dias, abordarei uma variedade de tópicos, desde os conceitos básicos de Java até projetos mais avançados. Vou explorar áreas como


# Como Funciona

- Diariamente: Cada dia de código será documentado com o que foi feito, aprendido e os desafios encontrados.
- Projetos: Haverá pequenos projetos para colocar em prática o que foi aprendido.
- Reflexões: A cada semana, farei uma reflexão sobre o progresso, o que foi mais desafiador e como estou evoluindo.

# Estrutura do Repositório
- DiaX/: Pasta contendo o código do dia X, onde cada dia de código terá sua própria pasta.
- Projeto/: Diretório com os projetos mais significativos desenvolvidos ao longo do desafio.
- Resumo Semanais/: Arquivos com reflexões e aprendizados de cada semana.

# Objeto Final
Ao término dos 100 dias, espero ter um portfólio robusto de projetos Java, além de um entendimento mais profundo da linguagem e das melhores práticas de desenvolvimento. Este repositório servirá como um registro da minha jornada e uma fonte de aprendizado para outros que também estão interessados em melhorar suas habilidades em Java.


## 💻Roteiro de Estudos
| Dia  | Tema       | Descrição |
| :---------------- | :--------- | :--------- |
| `01` | `Olá Mundo` | `Escreva um programa que imprime "Hello World! no console, Adicione comentários de linha única e multilinha ao seu código.` |
| `02` | `Variáveis e Tipos de Dados` | `Declare inicialize variáveis para vários tipos de dados, incluindo inteiro (int), número de ponto flutuante (double) e (String). Em seguida, imprima cada variável no console. .` |
| `03` | `Entrada e Saída` | `Escreva um programa que leia a entrada do usuário e imprima-a no console. 2- Modifique o programa para ler e imprimir diferentes tipos de dados de entradas (inteiros, números de ponto flutuante, strings)  3- Escreva um programa para ler a entrada do usuário usando a classe Scanner.` |
| `04` | `Operações Aritméticas Básicas` | `1 Escreva um programa que declare duas variáveis inteiras e execute operações aritméticas básicas (adição, subtração, multiplicação, divisão) nelas. Imprima os resultados no console. 2. Escreva um programa que calcule a área de um retângulo. Peça ao usuário para inserir o comprimento (inteiro) e a largura (inteiro) do retângulo, calcular a área (comprimento * largura) e imprimir o resultado. 3. Modifique o programa acima para ler números como comprimento e largura e gerar a área com duas casas decimais` |
| `05` | `Operadores de Atribuição Compostos` | `1- Escreva um programa que utilize operadores de incrimento(++) e decremento(--). 2- Escreva um programa que utilize os seguintes operadores compostos +=, -=, *=, /= e %=.` |
| `06` | `Conversão de Tipos` | `Escreva um programa que execute as seguintes tarefas: -Converter uma string em umm inteiro -Converter uma String em um Float -Converter um inteiro em uma String usando o método valueOf() Converter um inteiro em uma String usando o método toString()` |
| `07` | `Operações Matemáticas` | `Escreva um programa que execute as seguintes tarefas usando a classe Math: -Máximo de 2 números -Mínimo de 2 números -Raiz quadrada de um número -Piso e teto de um flutuador -Valor abs de um duplo` |
| `08` | `Declarações Condicionais (if-else)` | `1- Escreva um programa que leia um inteiro como entrada do usuário e imprima se o número é ímpar ou par no console. 2- Escreva um programa que receba três números como entrada e imprima o maior entre eles 3- Escreva um programa que verifique se um determinado ano de entrada é um ano bissexto ou não.` |
| `09` | `Declaração Switch` | `1- Escreva um programa que leia a porcentagem e então imprima a nota correspondente (A, B, C, D ou F) usando a instrução switch` |
| `10` | `Loops (for, while, do-while)` | `1- Escreva um programa que leia um inteiro de entrada 'n' e imprima a soma dos inteiros de 1 a 'n' usando três tipos diferentes de loops: for, while e do-while. 2- Escreva um programa que leia um inteiro de entrada 'n' e imprima a soma dos inteiros pares de 1a 'n' (use continue ). 3- Escreva um programa que gere números aleatórios entre 1 e 100 até encontrar um número divisível por 5 e 7. Quando o número for encontrado, imprima-o encerre o loop usando a instrução break.` |
| `11` | `Manipulação de Cordas` | `-Comprimento da string: Calcula e imprime o comprimento de uma determinada string. -Concatenação de strings: concatena duas strings e imprime o resultado. -Comparação de strings: compare duas strings e imprima se elas são iguais ou não. -Conversão de maiúsculas e minúsculas de string: Converte uma string específica para maiúsculas e minúsculas e imprime o resultado. -Corte de string: remove espaços em branco inicias e finais de uma determinada string e imprime o resultado. -Extração de substring: Extraia uma substring de uma string fornecida e imprima-a -Divisão de strings: divide uma string fornecida em substrings com base em um delimitador e imprime as substrings resultantes. -Reversão de string: inverte uma string fornencida e imprime o resultado -Pesquisa de string: Pesquise um substring específica dentro de uma string fornecida e imprima se ela foi encontrada ou não` |
| `12` | `StringBuffer e StringBuilder` | `Escreva um programa que execute as seguintes tarefas: -inicialize um StringBuffer com valor "Hello" e adicione um espaço e "World" e imprima-o -incialize um StringBuilder com o valor "Hello" e adicione um espaço e "World" e imprima-0.` |
| `13` | `Matrizes` | `1. Escreva um programa para declarar e inicializar um array de inteiros. Faça um loop pelo array e calcule a média dos números. 2. Escreva um programa para copiar elementos de um array para outro array` |
| `14` | `Matrizes 2D` | `Escreva um programa que execute as seguintes tarefas: -inicialize um 3x4 2D-Array com os valores acima -Escreva um função que receba um array 2D como parâmetro e retorne um array com um valor máximo de cada linha. Para o exemplo acima, a função deve retornar o array [78, 90, 89] -Escreva uma função que recebaa uma matriz 2D nxm como parâmetro e retorne uma transformação mxn da matriz 2D de entrada` |
| `15` | `ArrayList` | `Escreva um programa que execute as seguintes tarefas: -inicialize um ArrayList chamado weekdays que contém strings de segunda a domingo -imprima todos os elementos na ArrayList weekdays -Classifique os elementos dos dias da semana em ordem alfabética crescente e imprima-os novamente -Classifique os elementos dos dias da semana em ordem alfabética em ordem decrescente e imprima-os novamente -Escreva uma função que receba um ArrayList e um charater como parâmetros e remova todos os elementos do arraylist que começam com o caractere fornecido. Por exemplo: se os parâmetros forem dias da semana e o caractere 't', então terça-feira e quinta-feira devem ser removidos do arraylist weekdays` |
| `16` | `Enums` | `Escreva um programa que execute as seguintes tarefas: -Inicialize uma enumeração chamada weekdays que contém strings de segunda a domingo -Imprima todos os elementos no enum weekdays` |
| `17` | `Data` | `Escreva um programa que execute as seguintes tarefas: -Inicialize uma variável de data com data atual como valor e imprima-a -Imprima o tempo da época atual -Inicializar uma variável de data com a data 7 dias antes do dia atual -Compare 2 datas com os métodos antes , depois e compareTo -Imprima a data atual nos seguintes fusos horários: GMT, CST, IST` |
| `18` | `Funções` | `1. Defina uma função (método) " isPrime" que recebe um inteiro como parâmetro e retorna um booleano (true/false) se o inteiro for um número primo 2. Defina uma função que recebe uma string como parâmetro e retorna o número de vogais que ela contém` |
| `19` | `Geração de Números Aleatórios` | `1. Escreva um programa para gerar um número aleatório. 2. Escreva um programa para gerar um número aleatório entre dois inteiros` |
| `20` | `Olá Mundo` | `Escreva um programa que execute as seguintes tarefas: -Inicializar um conjunto de valores de string e adicionar itens a ele -Verifique se um valor existe no conjuto -Remover um valor do conjunto -Imprima o número de valores (tamanho) no conjunto -Imprima o todos os valores um por um, percorrendo o conjunto -Imprima todos os valores um por um, percorrendo o conjunto -Remover todos os valores do conjunto` |
| `21` | `HashMap` | `Escreva um programa que execute as seguintes tarefas: -Inicialize um HashMap chamado alunos com chave Integer e valor String para salvar números de matricula e nomes de alunos -Adicionar itens aos alunos do HashMap -Acesse itens do HashMap dos alunos usando get() e imprima o nome dos alunos -Verifque se uma chave existe no HashMap -Remover um item do HashMap -Imprima o número de elementos (tamanho) no HashMap -Imprima todos os nomes dos alunos um por um, percorrendo o HashMap usando keySet() -Imprima todos os nomes dos alunos um por um, percorrendo o HashMap usando entrySet()` |
| `22` | `Pilha` | `Escreva um programa que execute as seguintes tarefas: -Inicializar uma pilha de valores de string e enviar itens pra ela -Remova o elemento superior da pilha -Imprima o elemento superior da pilha sem removê-lo -Verifique se um valor existe na pilha -Imprima o número de valores (tamanho) na pilha -Remover todos os valores da pilha` |
| `23` | `Fila` | `Escreva um programa que execute as seguintes tarefas: -Inicializar uma fila de valores de string e adicionar itens a ela -Remova o elemento head da fila -Imprima o primeiro elemento da fila sem removê-lo -Verifique se um valor existe na fila -Imprima o número de valores (tamanho) na fila -Remover todos os valores da fila.` |
| `24` | `Programação Orientada a Objetos (POO)` | `Passe este dia aprendendo os conceitos básicos de POO -Aprenda sobre os quatro princípios principais da POO (Encapsulamento, Herança, Polimorfismo, Abstração) e crie exemplos de cada um -Modificadores de acesso: público, privado e protegido -Diferença entre sobrecarga de método e substituição de método` |
| `25` | `Aula Simples` | `Crie uma classe básica com atribuitos e métodos. -Crie uma classe Usuário com os seguintes campos: id (int), nome, sobrenome, e-mail, cidade, país -Adicione um construtor para inicializar um objeto Usuário -Implementar getters e setters para acessar valores do usuário -Implementar um método getFullname() para obter o nome completo (ou seja, nome + sobrenome) do usuário.` |
| `26` | `Construtores` | `Implementar vários construtores para a classe User: -Com parâmetros (nome, sobrenome) Com parâmetros (e-mail) -Com parâmetros (nome, sobrenome, e-mail) -Instanciar objetos da classe User usando os construtores acima.` |
